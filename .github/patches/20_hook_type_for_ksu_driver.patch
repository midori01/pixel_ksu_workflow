From: midori01 <lv@lvlv.lv>
Date: Sat, 6 Jun 2026 11:42:25 +0900
Subject: [PATCH] Import hook type for KOWX712/KernelSU driver

---
 kernel/supercall/dispatch.c | 26 ++++++++++++++++++++++++++
 uapi/supercall.h            |  5 ++++-
 2 files changed, 30 insertions(+), 1 deletion(-)

diff --git a/kernel/supercall/dispatch.c b/kernel/supercall/dispatch.c
index 680816f7..f6dfe4d8 100644
--- a/kernel/supercall/dispatch.c
+++ b/kernel/supercall/dispatch.c
@@ -719,6 +719,26 @@ static int do_get_sulog_fd(void __user *arg)
     return ksu_install_sulog_fd();
 }
 
+static int do_get_hook_type(void __user *arg)
+{
+    struct ksu_hook_type_cmd cmd = { 0 };
+
+#ifdef CONFIG_KSU_SUSFS
+    const char *type = "Inline";
+#else
+    const char *type = "Kprobes";
+#endif
+
+    strscpy(cmd.hook_type, type, sizeof(cmd.hook_type));
+
+    if (copy_to_user(arg, &cmd, sizeof(cmd))) {
+        pr_err("get_hook_type: copy_to_user failed\n");
+        return -EFAULT;
+    }
+
+    return 0;
+}
+
 // IOCTL handlers mapping table
 // clang-format off
 static const struct ksu_ioctl_cmd_map ksu_ioctl_handlers[] = {
@@ -854,6 +874,12 @@ static const struct ksu_ioctl_cmd_map ksu_ioctl_handlers[] = {
         .handler = do_get_sulog_fd,
         .perm_check = only_root
     },
+    {
+        .cmd = KSU_IOCTL_HOOK_TYPE,
+        .name = "HOOK_TYPE",
+        .handler = do_get_hook_type,
+        .perm_check = manager_or_root
+    },
     {
         .cmd = 0,
         .name = NULL,
diff --git a/uapi/supercall.h b/uapi/supercall.h
index cae5da2f..284048f6 100644
--- a/uapi/supercall.h
+++ b/uapi/supercall.h
@@ -163,5 +163,8 @@ static const __u32 KSU_IOCTL_NUKE_EXT4_SYSFS = _IOC(_IOC_WRITE, 'K', 17, 0);
 static const __u32 KSU_IOCTL_ADD_TRY_UMOUNT = _IOC(_IOC_WRITE, 'K', 18, 0);
 static const __u32 KSU_IOCTL_SET_INIT_PGRP = _IO('K', 19);
 static const __u32 KSU_IOCTL_GET_SULOG_FD = _IOW('K', 20, struct ksu_get_sulog_fd_cmd);
-
+static const __u32 KSU_IOCTL_HOOK_TYPE = _IOC(_IOC_READ, 'K', 101, 0);
+struct ksu_hook_type_cmd {
+    char hook_type[32];
+};
 #endif
-- 
2.54.0

